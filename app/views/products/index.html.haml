%h2.center 商品一覧
.products.center
  .row
    - @products.each do |product|
      .col.s12.m6
        .card
          .card-image
            - if product.image.present?
              = image_tag product.image_url
            %span.card-title
            #{link_to product.name, product}(#{product.display_price})
          .card-content
            %p
              = product.description